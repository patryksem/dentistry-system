<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="#">System</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">

        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <ul class="navbar-nav ml-auto">

            {% if is_granted('IS_IMPERSONATOR') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ impersonation_exit_path(path('homepage') ) }}">Wróć do konta administratora</a>
                </li>
            {% else %}

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Wyloguj</a>
                </li>
            {% endif %}

        </ul>
        {% endif %}
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}

        <ul class="navbar-nav mr-lg-0">
            <li class="nav-item" style="color: rgba(255,255,255,.5)">
                {{ app.user.fullName }} ({{ app.user.roleName }})
            </li>
        </ul>
        {% endif %}


    </div>
</nav>
